const serverConfig = {
    domain: "localhost",
    port: "8080",
    route: {
        search: "search"
    },
    category: {
        movie: "movie"
    },
    action: {
        search: "search"
    }
};

export { serverConfig };